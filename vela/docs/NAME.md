# Name

**Vela**

Latin *velum*, a sail. Also the southern constellation — the ship’s canvas.

Two syllables. Easy to type. The binary is `vela`.

```bash
vela myproject.asgi:application --workers 4
```
